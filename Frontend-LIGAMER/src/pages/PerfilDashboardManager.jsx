import PerfilManager from "../componentes/PerfilManager";

export default function PerfilDashboardManager() {
    return (
        <PerfilManager title="Perfil Manager">
        </PerfilManager>
    );
}